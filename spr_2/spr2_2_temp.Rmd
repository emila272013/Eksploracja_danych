---
title: "Sprawozdanie 2 -- Część 2 (tymczasowe)"
subtitle: "Eksploracja danych"
author: "Emilia Porczyńska, Michał Szostak"
date: "`r Sys.Date()`"
output:
  bookdown::pdf_document2:
    toc: true
    fig_caption: yes
    fig_width: 5
    fig_height: 4
    number_sections: true
    keep_tex: false
header-includes:
  - \usepackage[utf8]{inputenc}
  - \usepackage[polish]{babel}
  - \usepackage{graphicx}
  - \usepackage{xcolor}
  - \usepackage{hyperref}
  - \usepackage{floatrow}
  - \floatsetup[table]{capposition=bottom}
  - \usepackage{caption}
  - \definecolor{setosa}{HTML}{833FE7}
  - \definecolor{versicolor}{HTML}{5D00BE}
  - \definecolor{virginica}{HTML}{A68DE5}
  # kolory do tekstu trochę przyciemnione żeby wizualnie wyglądały tak samo
  - \definecolor{afryka}{HTML}{AC7800}
  - \definecolor{azja}{HTML}{AB4400}
  - \definecolor{europa}{HTML}{007552}
  - \definecolor{amerykapn}{HTML}{005C92}
  - \definecolor{oceania}{HTML}{2C7DA8}
  - \definecolor{amerykapd}{HTML}{98506E}
  - \definecolor{antarktyda}{HTML}{B3B5B9} # :(
fontsize: 12pt
---

```{r setup, echo=FALSE, warning=FALSE, message=FALSE}
library(plot3D)
library(ggplot2)
library(factoextra)
library(corrplot)
library(knitr)
library(kableExtra)
library(dplyr)
library(showtext) # żeby były polskie znaki na wykresach
showtext_auto()   #

knitr::opts_chunk$set(
	fig.pos = "H",
	cache = TRUE,
	out.extra = ""
)
```

```{r setup-2, echo=FALSE, warning=FALSE, message=FALSE}
dane2 <- read.csv("uaScoresDataFrame.csv", stringsAsFactors = TRUE)

# kolory kontynentów (dla wykresów PCA)
kolory_kontynentow <- c(
  "Africa"        = "#E69F00",
  "Asia"          = "#D55E00",
  "Europe"        = "#009E73",
  "North America" = "#0072B2",
  "Oceania"       = "#56B4E9",
  "South America" = "#CC79A7"
)

# kolor tła legend
kolor_tla_legendy <- "#F5FDFF"
```

# Część 2 - Analiza składowych głownych

## Wstęp

W części drugiej będziemy pracować na zbiorze danych pobranych ze strony [\textcolor{magenta}{Kaggle}](https://www.kaggle.com/datasets/orhankaramancode/city-quality-of-life-dataset), pochodzącym oryginalnie z nieistniejącej już (przynajmniej pod tą domeną) strony `Teleport.org`. Zbiór zawiera wskaźniki opisujące jakość życia z wybranej grupy „najbardziej kreatywnych” miast z całego świata zebranych w celu pozwolenia osobom znalezć odpowiedniego miasta do życia według ich preferencji.

Wskaźniki dotyczą między innymi charakterystyk takich jak jak: warunki mieszkaniowe, koszty utrzymania, bezpieczeństwo, opieka zdrowotna, edukacja itp. Wszystkie wskaźniki są w skali od 0 do 10, gdzie większa wartość oznacza lepszy wynik.

Naszym celem będzie przeprowadzenie analizy składowych głównych (PCA, ang. *Principal Component Analysis*) wskaźników zawartych w zbiorze.

## Przygotowanie danych

Dane zawierają informacje o `r nrow(dane2)` miastach. Dla każdego z nich mamy `r ncol(dane2)` cech.

```{r tabela-cech-2, echo=FALSE, warning=FALSE, message=FALSE}
tab_cechy_2 <- data.frame(
  Typ = sapply(dane2, class),
  Opis = c(
    "identyfikator obserwacji",
    "nazwa obszaru miejskiego (UA -- ang. \\textit{urban area})",
    "kraj",
    "kontynent",
    "warunki mieszkaniowe",
    "koszty utrzymania",
    "ekosystem startupów",
    "dostępność kapitału inwestycyjnego",
    "połączenia komunikacyjne (transport międzymiastowy i międzynarodowy)",
    "codzienne dojazdy",
    "swoboda prowadzenia biznesu",
    "bezpieczeństwo",
    "opieka zdrowotna",
    "edukacja",
    "jakość środowiska",
    "kondycja gospodarki",
    "podatki",
    "dostęp do internetu",
    "oferta rozrywkowa i kulturalna",
    "tolerancja społeczna",
    "możliwości spędzania czasu na świeżym powietrzu"
  )
)

# escape=FALSE -- żeby \textit zadziałał -- wymaga ręcznego escape'u podkreśleń w nazwach
rownames(tab_cechy_2) <- gsub("_", "\\\\_", rownames(tab_cechy_2))

kable(tab_cechy_2,
      caption = "Typy oraz opisy cech",
      booktabs = TRUE,
      escape = FALSE,
      align = "l") %>%
  kable_styling(full_width = FALSE,
                latex_options = c("striped", "HOLD_position"),
                stripe_color = "gray!10") %>%
  column_spec(3, width = "9cm")
```

Identyfikator `X` na pewno nie będzie nas interesował, więc go usuwamy.

Aby ujednolicić konwencję nazw, nazwy cech `UA_Name`, `UA_Country` oraz `UA_Continent` zamieniamy odpowiednio na `UrbanArea`, `Country` i `Continent`. Pozostałe nazwy cech, w których spacje zostały zamienione przez wbudowany w R czytnik CSV na kropki również ujednolicamy do tej konwencji. W nazwie cechy `Leisure...Culture`, w której na kropkę został zamieniony także symbol „&”, powstały trzykropek zamieniamy na „And”.

```{r oczyszczanie-danych-2, echo=FALSE}
# usunięcie identyfikatora
dane2$X <- NULL

# ujednolicenie nazw zmiennych
names(dane2)[names(dane2) ==               "UA_Name"] <- "UrbanArea"
names(dane2)[names(dane2) ==            "UA_Country"] <- "Country"
names(dane2)[names(dane2) ==          "UA_Continent"] <- "Continent"
names(dane2)[names(dane2) ==        "Cost.of.Living"] <- "CostOfLiving"
names(dane2)[names(dane2) ==       "Venture.Capital"] <- "VentureCapital"
names(dane2)[names(dane2) ==   "Travel.Connectivity"] <- "TravelConnectivity"
names(dane2)[names(dane2) ==      "Business.Freedom"] <- "BusinessFreedom"
names(dane2)[names(dane2) == "Environmental.Quality"] <- "EnvironmentalQuality"
names(dane2)[names(dane2) ==       "Internet.Access"] <- "InternetAccess"
names(dane2)[names(dane2) ==     "Leisure...Culture"] <- "LeisureAndCulture"

dane2_ilo <- dane2[, sapply(dane2, is.numeric)]
```

Zmienione nazwy zmiennych możemy zobaczyć w poniższej tabeli – będącej częścią dalszych działań dążących do oczysczenia danych – podsumowującej wartości nieusuniętych zmiennych:

```{r podsumowanie-danych-2, echo=FALSE, warning=FALSE, message=FALSE, results='asis'}
# Przygotowanie tabeli
tab_summary_2 <- summary(dane2)
tab_summary_2[is.na(tab_summary_2)] <- "" # usunięcie niepotrzebnych "NA" zaśmiecających tabelę
tab_summary_2 <- gsub("\\s*:\\s*", ": ", tab_summary_2) # naprawienie zepsutych dwukropków
tab_summary_2 <- gsub("\\(Other\\)", "\\(Inne\\)", tab_summary_2) # Inne zamiast Other

# Tabela jest szeroka więc ją łamiemy na 4 kawałki i wyświetlamy w tej szatańskiej pętli
kawalki <- list(1:5, 6:10, 11:15, 16:20)

for (i in seq_along(kawalki)) {
  kawalek_tabeli = tab_summary_2[, kawalki[[i]], drop = FALSE]
  kawalek_tabeli = kawalek_tabeli[rowSums(kawalek_tabeli != "") > 0, , drop = FALSE] # usunięcie pustych wierszy

  caption_i = if (i == length(kawalki)) "Podsumowanie danych" else NULL # caption tylko pod ostatnim kawałkiem
  tabela = kable(kawalek_tabeli, booktabs = TRUE, caption = caption_i) %>%
    kable_styling(latex_options = c("striped", "scale_down", "HOLD_position"))

  print(tabela) # kabele w pętlach się same nie printują więc musimy to zrobić sami
}
```

Jak widać, wszystkie zmienne zawierają wartości zgodne z naszymi oczekiwaniami. Nie występują też żadne braki danych.

Mamy 3 cechy jakościowe reprezentujące nazwę miasta, kraju i kontynentu oraz 17 cech ilościowych zawierających wartości wskaźników jakości życia.

W podsumowaniu danych jakościowych widzimy że mamy informacje o miastach znjadujacych sie na 6 kontynetach (niestety bez \textcolor{antarktyda}{Antarktydy}). Powyższa tabela wskazuje nam ze najwiecej miast mamy z \textcolor{europa}{Europy} (`r sum(dane2$Continent == "Europe")`), następnie z \textcolor{amerykapn}{Ameryki Północnej} (`r sum(dane2$Continent == "North America")`). Łącznie mamy informacje o `r length(unique(dane2$UrbanArea))` miastach z `r length(unique(dane2$Country))` państw, co świadczy o dużej różnodrodności.

W tabeli widzimy też, że nie wszystkie cechy ilościowe przyjmują wartości skrajne – 0 oraz 10. Może to sugerować, że ich rozkłady się znacząco różnią.

W celu najlepszego wyznaczenia składowych głównych, zmienne powinny mieć podobną wariancję.

```{r box-plot-war, echo=FALSE, fig.width=6.5, fig.height=4.5, out.width="100%", fig.cap="Rozkład wartości cech ilościowych"}
par(mar = c(8, 4, 1, 1))
boxplot(dane2_ilo,
        col = rainbow(ncol(dane2_ilo)),
        xaxt = "n",
        range = 3, # bez tego za dużo jest traktowane jak outliery
        ylab = "wartość cechy")
text(x = seq_len(ncol(dane2_ilo)),
     y = par("usr")[3] - 0.3,
     labels = colnames(dane2_ilo),
     srt = 45, adj = c(1, 1), xpd = TRUE)
```

Widzimy, że zakres wartości zmiennych takich jak `Commute` i `Outdoors` jest dużo węższy niż przykładowo `VentureCapital`, `EnvironmentQuality`, czy `Education`. Wskazuje to na różnice w wariancji. Z tego powodu dokonujemy standaryzacji za pomocą wbudowanej w R funkcji `scale()`. Wynik tego działania znajduje sie na poniższym wykresie.

```{r box-plot-war-po-stand, echo=FALSE, fig.width=6.5, fig.height=4.5, out.width="100%", fig.cap="Rozkład wartości cech ilościowych po standaryzacji"}
dane2_ilo_stand <- scale(dane2_ilo)
par(mar = c(8, 4, 1, 1))
boxplot(dane2_ilo_stand,
        col = rainbow(ncol(dane2_ilo_stand)),
        xaxt = "n",
        range = 3, # bez tego za dużo jest traktowane jak outliery
        ylab = "wartość po standaryzacji")
text(x = seq_len(ncol(dane2_ilo_stand)),
     y = par("usr")[3] - 0.15,
     labels = colnames(dane2_ilo_stand),
     srt = 45, adj = c(1, 1), xpd = TRUE)
```

Musimy jednak pamiętać że wartości zmiennych po standardyzacji nie są już w skali 0–10.

## Wyznaczanie składowych głownych

Kolejnym naszym krokiem będzie wyznaczenie składowych głównych naszych zmiennych. Użyjemy do tego funkcji `prcomp()`.

W celu interpretacji otrzymanych składowych, analizujemy ich wektory ładunków (ang. *loadings*). Reprezentują one wkład każdej ze zmiennych na wartości składowych.

```{r pca-3, echo=FALSE}
pca_dane <- prcomp(dane2_ilo, scale.=TRUE, center=TRUE, retx=TRUE)

loadings_3 <- round(pca_dane$rotation[, 1:3], 3)
loadings_kolorowe <- apply(loadings_3, 2, function(kol) { # żeby były kolorki w tabeli +/-
  cell_spec(sprintf("%.3f", kol),
            color = ifelse(kol > 0, "#2166AC",
                    ifelse(kol < 0, "#B2182B", "black")))
})
rownames(loadings_kolorowe) <- rownames(loadings_3)

kable(loadings_kolorowe,
      booktabs = TRUE,
      escape = FALSE,
      align = "r",
      caption = "Wartości wektorów ładunków trzech pierwszych składowych głównych") %>%
  kable_styling(latex_options = c("striped", "HOLD_position"),
                stripe_color = "gray!10",
                full_width = FALSE)
```

Największy wpływ na interpretację składowych głównych mają zmienne o najwyższych wartościach bezwzględnych ładunków.

Najwyższe dodatnie wartości dla pierwszej składowej głównej (PC1) osiągają zmienne Housing oraz CostOfLiving, co wskazuje na silny związek tej składowej z kosztami życia i dostępnością mieszkań.
Z kolei najniższe (ujemne) wartości przyjmują zmienne Education, BusinessFreedom oraz EnvironmentalQuality.
Oznacza to, że PC1 można interpretować jako wymiar kontrastu pomiędzy wysokimi kosztami życia a poziomem rozwoju społeczno-gospodarczego.

W przypadku drugiej składowej głównej (PC2) najwyższe dodatnie wartości osiągają zmienne Safety, Healthcare, EnvironmentalQuality oraz Tolerance, które opisują szeroko rozumianą jakość życia.
Najniższe wartości przyjmują natomiast zmienne Startups, VentureCapital oraz LeisureAndCulture, związane z aktywnością innowacyjną i gospodarczą.
W związku z tym PC2 można interpretować jako wymiar przeciwstawiający wysoką jakość życia dynamice innowacyjno-gospodarczej.

Dla trzeciej składowej głównej (PC3) najwyższą dodatnią wartość przyjmuje zmienna Economy, natomiast niskie (ujemne) wartości obserwujemy m.in. dla zmiennych Commute, TravelConnectivity, CostOfLiving oraz Safety.
Oznacza to, że PC3 można interpretować jako wymiar odzwierciedlający sytuację gospodarczą w zestawieniu z wybranymi aspektami komfortu życia. Wyższe wartości tej składowej mogą wskazywać na lepsze warunki ekonomiczne, ale jednocześnie mniej korzystne warunki codziennego funkcjonowania.

## Zmienność odpowiadająca poszczególnym składowym

W kolejnym etapie analizy zbadaliśmy, jaki procent całkowitej zmienności (wariancji) danych jest wyjaśniany przez poszczególne składowe główne.

Na pierwszym wykresie przedstawiono wariancję poszczególnych składowych. Zgodnie z założeniami metody PCA, wartości wariancji maleją wraz z kolejnymi składowymi, co potwierdza, że pierwsze z nich zawierają najistotniejszą informację o strukturze zbioru danych.


```{r wykres-war-pca, echo=FALSE, fig.cap="Wariancje poszczególnych składowych głównych"}
plot(pca_dane,
     main = "",
     xaxt = "n")

axis(1,
     at = 1:length(pca_dane$sdev),
     labels = paste0("PC", 1:length(pca_dane$sdev)),
     las = 2)
```

```{r wykres-war-skumulowany, echo=FALSE, fig.cap="Skumulowana wariancja (w \\%) wyjaśniana przez kolejne składowe główne"}
variance <- 100*(pca_dane$sdev^2)/sum(pca_dane$sdev^2)
barplot(cumsum(variance),
        main = "",
        ylab = "skumulowana wariancja (%)",
        names.arg = paste0("PC", 1:length(variance)), las = 2)
kolor_80 <- "#3FB683" # oklch(~70% 0.14 155) – zielony
kolor_90 <- "#E07566" # oklch(~70% 0.14 25)  – koral, ta sama jasność/chroma
abline(h = 80, col = kolor_80, lty = 2, lwd = 2)
abline(h = 90, col = kolor_90, lty = 2, lwd = 2)
legend("right", legend = c("80%", "90%"), lwd = 2, lty = 2,
       col = c(kolor_80, kolor_90), bg = kolor_tla_legendy)
```

Aby móc odpowiedzieć na pytanie, ile składowych głównych jest potrzebnych do wyjaśnienia 80% oraz 90% całkowitej zmienności, na powyższym wykresie zaznaczamy odpowiednio poziomy 80% i 90%. Na podstawie tego wykresu możemy wyznaczyć liczbę składowych potrzebnych do wyjaśnienia odpowiednich wartości procentowych wariancji.

Do wyjaśnienia 80% wariancji potrzebnych jest 7 składowych głównych, natomiast do wyjaśnienia 90% wariancji potrzebnych jest 10 składowych głównych.

### Wizualizacja danych wielowymiarowych

```{r 2d-kontynenty-pca, echo=FALSE, fig.cap="Rzut danych na dwie pierwsze składowe główne, kolory wg kontynentu"}
kontynenty <- as.factor(dane2$Continent)
plot(pca_dane$x[, 1], pca_dane$x[, 2],
     col = kolory_kontynentow[as.character(kontynenty)],
     pch = 16,
     xlab = "PC1",
     ylab = "PC2",
     main = "")

legend("topright",
       legend = names(kolory_kontynentow),
       col = kolory_kontynentow,
       pch = 16,
       cex = 0.7,
       bg = kolor_tla_legendy)
```

Na powyższym wykresie przedstawiono rozkład miast w przestrzeni dwóch pierwszych składowych głównych, które można interpretować odpowiednio jako koszty życia (PC1) oraz poziom rozwoju i jakość życia (PC2).

Punkty na wykresie są pogrupowane według kontynentów, co pozwala na analizę zróżnicowania geograficznego.

Miasta europejskie koncentrują się głównie w lewej górnej części wykresu. Oznacza to, że wiele z nich charakteryzuje się relatywnie niższymi kosztami życia (ujemne wartości PC1) przy jednocześnie wysokim poziomie rozwoju oraz jakości życia (wysokie wartości PC2), obejmującej m.in. bezpieczeństwo, opiekę zdrowotną oraz jakość środowiska.

Miasta Ameryki Północnej wykazują większe zróżnicowanie i są bardziej rozproszone na wykresie, z widoczną tendencją do niższych wartości PC2. Może to sugerować, że część tych miast w większym stopniu charakteryzuje się dynamiką innowacyjną oraz aktywnością gospodarczą (np. rozwój startupów), kosztem niektórych aspektów jakości życia.

Miasta azjatyckie cechują się największym rozproszeniem na wykresie, co odzwierciedla dużą różnorodność gospodarczą i społeczną tego kontynentu. Obejmuje ona zarówno wysoko rozwinięte metropolie (np. Singapur czy Tokio), jak i miasta o niższym poziomie rozwoju, co przekłada się na szeroki zakres wartości obu składowych.

Miasta afrykańskie w analizowanej próbie koncentrują się częściej w obszarach o wyższych wartościach PC1. W kontekście przyjętej interpretacji oznacza to, że czynniki związane z kosztami życia i dostępnością mieszkań odgrywają w ich przypadku istotniejszą rolę, przy jednocześnie niższych wartościach wskaźników związanych z jakością życia.

```{r biplot-3, echo=FALSE, fig.cap="Biplot PCA z elipsami ufności (95\\%) dla kontynentów"}
fviz_pca_biplot(pca_dane,
                label = "var",
                habillage = dane2$Continent,
                addEllipses = TRUE,
                ellipse.level = 0.95,
                repel = TRUE,
                select.var = list(contrib = 10),
                col.ind = "grey50",
                pointsize = 2,
                col.var = "black",
                title = "")
```
Na wykresie biplot przedstawiono podział miast według kontynentów wraz z naniesionymi wektorami zmiennych opisujących ich charakterystyki. Strzałki reprezentują kierunek i siłę wpływu poszczególnych zmiennych na składowe główne, co umożliwia jednoczesną analizę zależności między miastami oraz badanymi cechami.

Miasta Europy oraz Ameryki Północnej znajdują się po stronie odpowiadającej wyższym wartościom zmiennych związanych z poziomem rozwoju społeczno-gospodarczego. Z kolei miasta z Afryki, Ameryki Południowej oraz części Azji sytuują się po przeciwnej stronie osi Dim1, co sugeruje niższe wartości tych wskaźników w analizowanym zbiorze danych.
Widoczna separacja regionów potwierdza, że PCA skutecznie identyfikuje globalne różnice w poziomie rozwoju miast.


```{r 2d-skrajne-miasta, echo=FALSE, fig.cap="Rzut danych na dwie pierwsze składowe główne -- Afryka na tle pozostałych kontynentów"}
dist <- sqrt(pca_dane$x[,1]^2 + pca_dane$x[,2]^2)

idx <- order(dist, decreasing = TRUE)[1:4]
kolory_outliers <- c("red", "blue", "green", "purple", "orange")

moje_pozycje <- c(1, 2, 4, 3) 

plot(pca_dane$x[, 1], pca_dane$x[, 2], col = "lightgray", pch = 16, 
     main = "PCA – miasta skrajne", xlab = "PC1", ylab = "PC2")

points(pca_dane$x[idx, 1], pca_dane$x[idx, 2], col = kolory_outliers, pch = 19, cex = 1.5)
# text(pca_dane$x[idx, 1], pca_dane$x[idx, 2],
#      labels = dane2$UrbanArea[idx], # Zmieniono na dane2
#      pos = moje_pozycje, 
#      col = kolory_outliers,
#      cex = 0.8)

legend("topright", legend = dane2$UrbanArea[idx], col = kolory_outliers, pch = 19, title = "Miasta skrajne")
```
Na wykresie PCA widzimy, że miasta zlokalizowane w lewym dolnym rogu przestrzeni (niskie wartości PC1 i PC2) tworzą grupę o szczególnych cechach strukturalnych. Są to głównie metropolie z Europy i Ameryki Północnej, które charakteryzują się wysokimi kosztami życia oraz rozwiniętym rynkiem nieruchomości. Jednocześnie ich relatywnie niskie wartości PC2 sugerują, że mimo wysokiego poziomu bezpieczeństwa i jakości usług publicznych, mogą one cechować się mniejszą dynamiką innowacyjno-gospodarczą lub ograniczoną dostępnością pewnych aspektów życia miejskiego, takich jak przystępność kultury czy elastyczność rynku.

Lagos, położone w Nigerii (Afryka Zachodnia), znajduje się na przeciwległym biegunie osi PC1 względem wielu miast zachodnich, co czyni je jednym z najbardziej oddalonych punktów w tym wymiarze. Miasto to reprezentuje dynamicznie rozwijającą się metropolię Globalnego Południa, zmagającą się z wyzwaniami gwałtownej urbanizacji, presją demograficzną oraz ograniczeniami infrastrukturalnymi. Jego pozycja w przestrzeni PCA sugeruje, że czynniki takie jak nierówności społeczne, ograniczona dostępność usług publicznych (edukacja, opieka zdrowotna) oraz jakość środowiska w istotny sposób odróżniają je od metropolii Europy i Ameryki Północnej.

```{r 3d-europa-i-usa, echo=FALSE, fig.width=6, fig.height=5, out.width="100%", fig.cap="Rzut danych na trzy pierwsze składowe główne -- wyróżnione: Europa i Ameryka Północna"}

nowe_kolory <- c(
  "grey80",                            # 1. Africa - wygaszony
  "grey80",                            # 2. Asia - wygaszony
  kolory_kontynentow["Europe"],        # 3. Europe - wyraźny
  kolory_kontynentow["North America"], # 4. North America - wyraźny
  "grey80",                            # 5. Oceania - wygaszony
  "grey80"                             # 6. South America - wygaszony
)

# Zwiększ marginesy na legendę
par(mar = c(2, 2, 2, 6))

scatter3D(pca_dane$x[, 1], pca_dane$x[, 2], pca_dane$x[, 3],
          xlab = "PC1", ylab = "PC2", zlab = "PC3",
          colvar = as.numeric(dane2$Continent),
          col = nowe_kolory,        # <-- UŻYWAMY NOWEJ PALETY
          pch = 16,
          cex = 1.3,                # Powiększenie punktów dla czytelności
          bty = "g",                # Dodaje "siatkę" (grid)
          theta = 45, phi = 15,     # Zmieniony kąt dla lepszej separacji
          colkey = FALSE,
          main = "")

# Legenda poza obszarem wykresu
legend("topright", inset = c(-0.25, 0), xpd = TRUE,
       legend = levels(dane2$Continent),
       col = nowe_kolory,           # <-- UŻYWAMY NOWEJ PALETY
       pch = 16,
       bg = kolor_tla_legendy,
       title = "Kontynenty")
```

Na trójwymiarowym wykresie PCA przedstawiono rozmieszczenie miast względem trzech głównych składowych: PC1, PC2 oraz PC3, ograniczając analizę do miast Europy i Ameryki Północnej. Każda z osi reprezentuje odmienny wymiar jakości życia i rozwoju społeczno-gospodarczego.

Analiza rozmieszczenia miast w tej przestrzeni pokazuje wyraźne różnice regionalne. Miasta Ameryki Północnej częściej lokują się w obszarach wyższych wartości PC3, co sugeruje relatywnie silniejszą sytuację gospodarczą w porównaniu do miast europejskich. Jednocześnie miasta europejskie częściej osiągają wyższe wartości w wymiarach jakości życia (PC2), co wskazuje na większy nacisk na bezpieczeństwo, ochronę zdrowia i jakość środowiska. W rezultacie obie grupy miast różnią się profilem rozwoju: Ameryka Północna jest bardziej zorientowana na dynamikę gospodarczą, natomiast Europa na zrównoważoną jakość życia.

## Korelacja zmiennych

Następnie, w celu porównania zależności między zmiennymi opisanymi przez składowe PCA, zrobiliśmy dwuwykres obejmujący macierz korelacji oraz biplot PCA.

```{r corr-pca-vars, echo=FALSE, fig.cap="Korelacje cech ze składowymi głównymi"}
correlation.matrix <- cor(dane2_ilo)
corrplot(correlation.matrix)
```

```{r corr-cech, echo=FALSE, fig.cap="Macierz korelacji cech ilościowych"}
# correlation.matrix <- cor(dane2_ilo)
# corrplot(correlation.matrix)
```

```{r biplot-1, echo=FALSE, fig.cap="Biplot PCA -- 10 cech o największym wkładzie"}
fviz_pca_biplot(pca_dane,
                geom.ind = "point",   # tylko punkty
                col.ind = "grey70",   # jasne punkty
                col.var = "blue",
                repel = TRUE,
                select.var = list(contrib = 10), # tutaj ile cech chcemy
                title = "")
```
Analiza macierzy korelacji wykazała istotne zależności pomiędzy zmiennymi. W szczególności zaobserwowano silną dodatnią korelację między kosztami życia a dostępnością mieszkań, natomiast ujemne zależności występują między kosztami życia a wskaźnikami jakości środowiska oraz poziomu rozwoju społeczno-gospodarczego.

Wykres biplot PCA potwierdza te zależności w ujęciu geometrycznym. Zmienne silnie dodatnio skorelowane są reprezentowane przez wektory skierowane w podobnym kierunku, natomiast zmienne o korelacji ujemnej znajdują się po przeciwnych stronach wykresu względem siebie. Dodatkowo biplot umożliwia analizę zależności pomiędzy samymi zmiennymi — małe kąty między wektorami (np. Housing i CostOfLiving) wskazują na silną korelację dodatnią, natomiast przeciwne kierunki wektorów (np. Housing i Education) świadczą o korelacji ujemnej.

Wnioski uzyskane z obu metod są spójne i wzajemnie się uzupełniają. Macierz korelacji dostarcza precyzyjnych wartości liczbowych opisujących siłę zależności, natomiast biplot PCA pozwala na ich intuicyjną interpretację geometryczną, umożliwiając jednoczesną analizę relacji między zmiennymi oraz ich projekcji w przestrzeni składowych głównych.

## Końcowe wyniki

W przeprowadzonych analizach PCA udało się zaobserwować wyraźną strukturę danych, w której pierwsze dwie składowe główne opisują najważniejsze wymiary zróżnicowania miast: poziom kosztów życia (PC1) oraz jakość życia i rozwój społeczno-gospodarczy (PC2). Dodatkowo trzecia składowa (PC3) wnosi informację o sytuacji gospodarczej, jednak jej znaczenie jest mniejsze niż dwóch pierwszych komponentów.

Z analizy wynika, że już dwie składowe główne są wystarczające do uzyskania dobrej, czytelnej reprezentacji danych, ponieważ wyjaśniają one znaczną część całkowitej wariancji i pozwalają na wyraźne rozróżnienie grup miast. Dodanie trzeciej składowej poprawia szczegółowość interpretacji, ale nie jest kluczowe dla ogólnego obrazu struktury danych.

Standaryzacja danych miała istotny wpływ na wyniki analizy. Dzięki niej wszystkie zmienne zostały sprowadzone do porównywalnej skali, co zapobiegło dominacji cech o dużych wartościach liczbowych (np. kosztów życia). W efekcie PCA lepiej odzwierciedla rzeczywiste relacje między zmiennymi, a uzyskane składowe są bardziej zrównoważone.

